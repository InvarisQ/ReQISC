# Benchmarks

This directory stores manifests and examples for future benchmark workflows. The files here are not performance results.

A benchmark result should only be added when it includes enough information to reproduce the claim: package version, input workload, hardware profile, pass configuration, environment, random seeds where relevant, and result provenance.
